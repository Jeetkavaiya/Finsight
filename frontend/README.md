# FinSight frontend

React + Tailwind chat UI. Scaffolded on Day 9 with:

```bash
npm create vite@latest . -- --template react
npm install
npm run dev
```

It will call the backend `/query` endpoint and render answers with clickable
citations. Deployed to Vercel.
